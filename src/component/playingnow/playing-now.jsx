import styled from "styled-components";

const PlayNow = styled.div({

})

const PlayingNow = () => {
    return ( 
        <PlayNow>
            
        </PlayNow>
    );
}
 
export default PlayingNow;